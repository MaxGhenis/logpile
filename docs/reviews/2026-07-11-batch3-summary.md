# Batch 3 — publish gate, scanner, visibility, and scale (implemented)

Implements B5, B6, B7, H1, H2, H6, H8, M4, M5, M7, and M8 from
[2026-07-11-sol-full-review.md](2026-07-11-sol-full-review.md), following the
reviewer's scoped directions for B6, H1, and M8. Changes are left uncommitted
in the working tree for reviewer commit. No Git write command or full-corpus
resync was run.

## Findings

| Finding | Files touched | What changed | Test evidence |
|---|---|---|---|
| B5 | `logpile/parsers.py`, `logpile/db.py`, `logpile/sync.py`, `logpile/publish.py`, `web/src/lib/parsers.ts`, `web/src/app/sessions/[id]/page.tsx`, `web/src/lib/types.ts`, `tests/test_parsers.py`, `tests/test_publish.py`, `tests/test_web_transcript_streaming.py` | Python ingestion now iterates JSONL instead of retaining whole-record lists. Cardinality-dependent Claude and Codex state (messages, tools, result lookup, and paths) spills to mode-0600 temporary SQLite sequences; sync annotation, activity counting, claims, tools, and path aggregation stay streaming/disk-backed without dropping rows. Publish review hashes, scans, and stages exact bytes in one bounded pass. Next reads 64 KiB chunks, caps a request at 4 MiB/100 turns, paginates by byte cursor, and advances across records larger than the cap. Verified public-artifact hashes are cached in a bounded LRU keyed by opened inode/size/mtime/ctime identity, avoiding a multi-gigabyte rehash on every cursor page. | A 32+ MiB ingest fixture remains below 8 MiB. High-cardinality fixtures preserve 20,000 Claude messages/tools + 40,000 paths and 15,000 Codex tools + 30,000 paths below 12 MiB at both parser and sync sinks. `tests/test_parsers.py`: 44 passed. Next streaming: 2 passed, including a 9 MiB record followed by a reachable valid turn. |
| B6 (scoped) | `logpile/db.py`, `logpile/sync.py`, `logpile/cli.py`, `logpile/publish.py`, `logpile/web/app.py`, `web/src/lib/config.ts`, `web/src/app/publish/page.tsx`, `README.md`, `tests/test_visibility_artifacts.py`, `tests/test_contract.py`, `tests/test_rules.py`, `tests/test_sync.py`, `tests/test_web.py` | The low-level normalizer rejects missing/unknown values closed. Every effective **session** visibility change—manual command, review, rule/default recomputation, sync upsert, marker, drift, and migration—uses one guarded/audited transition API. Public requires a successful review explicitly approved for public and bound to the current transcript and metadata hashes. Private→unlisted remains review-free but emits and records the exact local/link-only warning; public mode never serves unlisted. Missing/invalid legacy values enter private and are then repaired through the same audited guard. Public profiles are exposed only while at least one current reviewed-public session exists. README and UI use the same promise. | `tests/test_visibility_artifacts.py`: 17 passed. Coverage includes invalid stored values, legacy migration audit, command/rule/default gates, unlisted warnings, approval-class separation, public-mode hiding, and profile metadata revocation. Contract, rules, sync, and web suites pass in the full battery. |
| B7 | `logpile/db.py`, `logpile/publish.py`, `logpile/cli.py`, `logpile/sync.py`, `logpile/parsers.py`, `logpile/web/app.py`, `web/src/lib/db.ts`, `web/src/lib/parsers.ts`, `web/src/app/sessions/[id]/page.tsx`, `web/src/lib/types.ts`, `tests/test_visibility_artifacts.py`, `tests/test_contract.py`, `tests/test_sync.py`, `tests/test_web.py` | Approval records the full reviewed SHA-256, reviewed metadata SHA-256, successful review row, and a mode-0600 hash-addressed artifact under `shared/.published/<session>/<sha>.jsonl`. Staging and final copies are hash-verified. Transcript or rendered-metadata drift freezes the old artifact, revokes to unlisted, and requeues the new revision—even for a same-size rewrite with restored mtime or a rotated-only parser backfill. Public Flask and Next serving accept only the managed reviewed artifact, reject corrupt files and symlinked roots/components, and parse from the same verified `O_NOFOLLOW` descriptor/FileHandle to close verify/reopen swaps. Display name, bio, and avatar URL are review-bound; editing them revokes affected publications and hides the public profile until re-review. | Regressions cover immutable reapproval history, same-size/mtime drift, metadata-only drift, rotated-only drift, profile drift, corrupt/symlink artifacts and roots, and an artifact rename/symlink swap after verification. Flask and Next visibility tests pass. |
| H1 (scoped) | `logpile/db.py`, `logpile/sync.py`, `tests/test_sync.py`, `tests/test_visibility_artifacts.py` | The archival shared/private-copy design is retained. Sync reports planned copy count/volume and free space before copying, and aborts clearly when space is insufficient. A copy must match the full source SHA-256 before source hash/size/mtime can advance. Failures persist `sync_copy_retries`, force the next retry, and preserve prior metadata/artifacts. Legacy private rows missing an archive now enter the plan and heal through the same verified mode-0600 private-copy path. | Tests inject corrupt copies, assert retry persistence and unchanged source metadata, then verify successful repair/cleanup. Free-space failure starts no copies or rows. Legacy private healing covers failed verification and successful retry. |
| H2 | `logpile/publish.py`, `logpile/db.py`, `logpile/web/app.py`, `web/src/lib/db.ts`, `web/src/app/sessions/[id]/page.tsx`, `web/src/app/publish/review/[id]/page.tsx`, `web/src/lib/types.ts`, `README.md`, `tests/test_publish.py`, `tests/test_visibility_artifacts.py`, `tests/test_web.py` | Scanner coverage now includes Slack xox tokens, JWTs, Google `AIza`, Stripe live keys, credentialed connection URIs, Bearer/Basic auth, npm/PyPI/registry tokens, classic and fine-grained GitHub tokens, SSNs, Luhn-valid cards, compact/formatted/international phones, Unix/Windows home paths, bounded PEM blocks, and high-entropy fallback. It scans all rendered session/profile text, including branch, titles, display name, bio, and avatar URL. Provider/PII validators and adversarial negatives control false positives. The UI now says exactly “No configured patterns detected; manual review required”. | `tests/test_publish.py`: 28 passed + 2 subtests. Positives cover every requested class, three compact phone forms, full PEM serialization masking, rendered metadata, and long cross-chunk paths. Negatives include malformed provider keys/JWTs, invalid SSN/card/phone values, counters, ordinary identifiers, and test Stripe keys. |
| H6 | `web/src/lib/db.ts`, `logpile/web/app.py`, `tests/test_web.py` | Context-explosion lineage applies visibility at the child `EXISTS`, initial leaf set, every recursive parent hop, leaf rejoin, and root rejoin in both Next and legacy Flask. An invisible ancestor stops the lineage from producing a public workstream. | Mixed-visibility root/child fixtures assert that private root goals, IDs, and summaries never appear through either implementation. `tests/test_web.py`: 27 passed. |
| H8 | `logpile/discovery.py`, `logpile/sync.py`, `logpile/backup.py`, `logpile/cli.py`, `README.md`, `tests/test_backup.py` | Sync and backup share one canonical root catalog for Claude, live/archived Codex, `.codex-2`, `.codex-3`, and OpenClaw homes. Backup also discovers safe DB-managed shared, private-archive, and reviewed artifacts even when the native path rotated away or was reused for newer bytes. Plans and pushes deduplicate exact content by full SHA-256. A missing or valid non-Logpile DB remains optional; an existing configured DB that is corrupt, unreadable, or fails its sessions query now aborts clearly instead of silently omitting sole-survivor artifacts. | `tests/test_backup.py`: 15 passed. Fixtures cover every root, byte duplicates, rotated-only rows, reused source paths, private/reviewed artifacts, missing/non-Logpile DBs, corrupt DBs, and injected query failures. |
| M4 | `logpile/publish.py`, `web/src/lib/types.ts`, `web/src/app/publish/review/[id]/page.tsx`, `tests/test_publish.py` | All rules use `finditer`. Evidence carries exact offsets and occurrence index/count, centers excerpts on the match, masks every detected value before serialization, and retains at most five examples per source/rule while preserving exact totals and omitted counts. Streaming commits matches by full regex start, retains left-boundary context, and rejects unbounded pattern widths, so cutoff-straddling matches are neither lost nor duplicated. | A 25,000-match fixture retains five masked examples, reports 24,995 omitted, and stays below 32 MiB. Exact safe-cutoff `AIza` and variable-width `sk-ant` regressions each produce one finding with correct offsets/count; the old implementation produced none. |
| M5 | `scripts/build_landing.py` | The module docstring explicitly documents intentional all-visibility aggregate publication and the requirement to retain the existing fine-print disclosure. No code gate was added; the landing fine print is unchanged. | Source inspection plus the full Python battery. |
| M7 | `logpile/db.py`, `tests/fixtures/migrations/legacy-identity-collision.sql`, `tests/test_migrations.py` | Legacy identity rebuilds enumerate columns with `PRAGMA table_info`, preserve every non-identity column/default/value, and resolve case-normalization collisions deterministically (`alice`, `alice-2`, …), independent of insertion order. Session, rule, and GitHub-daily owners are remapped collision-safely. Immediately before destructive replacement, on-disk DBs receive a non-overwriting, quick-checked mode-0600 SQLite snapshot; the rebuild is savepoint-protected and idempotent. | Collision fixture preserves custom user/session/rule/GitHub columns, cache/native fields, owners, and values. Snapshot retains the legacy schema/data, passes `quick_check`, is 0600, and is not duplicated on a second migration. |
| M8 (scoped) | `logpile/cli.py`, `pyproject.toml`, `README.md`, `tests/test_cli_backends.py` | Wheels remain Python-only. `logpile serve` checks for a real source checkout before DB/runtime work and exits with a clear clone/source-checkout message; no attempt was made to package either web app. README states the boundary. The core `sync`, `stats`, `search`, and `db-backup` commands remain wheel-safe. Project license metadata uses the current PEP 621 form accepted by the offline wheel build. | Final isolated offline smoke built and non-editably installed `logpile-0.1.0-py3-none-any.whl` from a temp source copy. From an unrelated cwd, all four core commands exited 0, source/backup SQLite `quick_check` returned `ok`, and `serve` exited 1 with the source-checkout/GitHub instruction. The 19-entry wheel contained Python and `.dist-info` only—no web/templates/static/site assets. |

## Verification

- `.venv/bin/python -m pytest tests/ -q`: **306 passed, 11 subtests passed**. The eight warnings are expected assertions of the new unlisted/public-review gate.
- Focused evidence: parsers 44 passed; publish/scanner 28 passed + 2 subtests; visibility/artifacts 17 passed; backup 15 passed; legacy/Next web 27 passed; Next transcript streaming 2 passed.
- `(cd web && bun run lint && bunx tsc --noEmit)`: passed.
- Plain `(cd web && bun run build)` reached the production compiler but the restricted sandbox could not fetch the three pre-existing Google Fonts (JetBrains Mono, Playfair Display, Plus Jakarta Sans); there was no code diagnostic. With `NEXT_FONT_GOOGLE_MOCKED_RESPONSES` supplying temporary offline CSS, `next build --webpack` compiled, type-checked, generated all 10 static pages, and collected traces successfully. The temporary mock was removed.
- Changed-file Ruff checks, Python compilation, and `git diff --check` passed.
- Final offline wheel evidence is under `/private/tmp/logpile-wheel-final.cKKZ2K`; it was built with `uv build --wheel --no-build-isolation --offline` and installed with `pip --no-index --no-deps` into an isolated venv.
- Generated `.egg-info`, temporary font mocks, and earlier index artifacts were removed from the working tree. No Git write command was used.

## Reviewer handoff

The full tree was independently re-audited after the cutoff, oversized-record,
profile-metadata, rotated-backfill, legacy-copy, and high-cardinality parser
fixes. No release-blocking finding remains. Profile visibility itself remains a
separate user-profile setting (outside the session transition audit), while
public profile exposure is fail-closed on the presence of a current
reviewed-public session.
